# Benchmark policy

Evaluation sets are published separately from the training corpus so they are not trained on
by accident. Held-out record identifiers, exclusion rules, and contamination guidance are
published alongside each benchmark release.
